[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "labgrid-prometheus-exporter-backend-wamp"
version = "0.1.0"
description = "labgrid-prometheus-exporter backend for labgrid's WAMP coordinator protocol (labgrid < 25.0)"
readme = "README.md"
requires-python = ">=3.13"
license = { text = "MIT" }
dependencies = [
    "labgrid-prometheus-exporter-core",
    "autobahn>=21.3.1",
    # Upper bound is deliberate here, unlike backend-grpc's: labgrid 25.0 is a
    # confirmed hard, backwards-incompatible break away from this protocol
    # (see CHANGES.rst), not a speculative guess.
    "labgrid>=24.0,<25.0",
]

[tool.uv.sources]
labgrid-prometheus-exporter-core = { workspace = true }

[dependency-groups]
dev = [
    "pytest>=8.4.0",
    "pytest-cov>=6.2.0",
    "ruff>=0.12.0",
    "ty>=0.0.67",
]

[tool.hatch.build.targets.wheel]
packages = ["src/labgrid_prometheus_exporter_backend_wamp"]

[tool.pytest.ini_options]
testpaths = ["tests"]
# see root pyproject.toml's addopts comment
addopts = "-q -p no:labgrid"
